// Widget tests are omitted in favour of unit tests for domain logic.
// See test/core/, test/repositories/, and test/providers/ for coverage.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
